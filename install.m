function install()
% Install benchmarks

installers = {...
  helpers.VlFeatInstaller(),...
  helpers.Installer(), ...
  benchmarks.repeatabilityBenchmark(), ...
  benchmarks.matchingBenchmark(), ...
  benchmarks.kristianEvalBenchmark()
  };

for installer=installers
  installer{:}.install();
end